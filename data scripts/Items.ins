DECLARE
    -- variable declarations if needed
    -- constant declarations if needed
    -- cursor declarations if needed
BEGIN
    -- Invoke APIs
    AMA_ITEMS_API.INSERT_ITEMS('I1', 'Noodles', 500.00, 400.00, 300.00);
END;
/
